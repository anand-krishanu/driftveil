# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class MachineActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Machine]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Machine.prisma().query_raw(
            'SELECT * FROM Machine WHERE id = ?',
            'bbadfchfja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Machine
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Machine.prisma().query_first(
            'SELECT * FROM Machine WHERE name = ?',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.MachineCreateInput,
        include: Optional[types.MachineInclude] = None
    ) -> _PrismaModelT:
        """Create a new Machine record.

        Parameters
        ----------
        data
            Machine record data
        include
            Specifies which relations should be loaded on the returned Machine model

        Returns
        -------
        prisma.models.Machine
            The created Machine record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Machine record from just the required fields
        machine = await Machine.prisma().create(
            data={
                # data to create a Machine record
                'name': 'ggciceaie',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.MachineCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Machine records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Machine record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Machine.prisma().create_many(
            data=[
                {
                    # data to create a Machine record
                    'name': 'bbehjachib',
                },
                {
                    # data to create a Machine record
                    'name': 'cadfabfehe',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.MachineWhereUniqueInput,
        include: Optional[types.MachineInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Machine record.

        Parameters
        ----------
        where
            Machine filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Machine model

        Returns
        -------
        prisma.models.Machine
            The deleted Machine record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        machine = await Machine.prisma().delete(
            where={
                'id': 'dgiiaaijj',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.MachineWhereUniqueInput,
        include: Optional[types.MachineInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Machine record.

        Parameters
        ----------
        where
            Machine filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Machine model

        Returns
        -------
        prisma.models.Machine
            The found Machine record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        machine = await Machine.prisma().find_unique(
            where={
                'id': 'bfaiacjjfc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.MachineWhereUniqueInput,
        include: Optional[types.MachineInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Machine record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Machine filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Machine model

        Returns
        -------
        prisma.models.Machine
            The found Machine record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        machine = await Machine.prisma().find_unique_or_raise(
            where={
                'id': 'eigcfgbif',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.MachineWhereInput] = None,
        cursor: Optional[types.MachineWhereUniqueInput] = None,
        include: Optional[types.MachineInclude] = None,
        order: Optional[Union[types.MachineOrderByInput, List[types.MachineOrderByInput]]] = None,
        distinct: Optional[List[types.MachineScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Machine records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Machine records returned
        skip
            Ignore the first N results
        where
            Machine filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Machine model
        order
            Order the returned Machine records by any field
        distinct
            Filter Machine records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Machine]
            The list of all Machine records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Machine records
        machines = await Machine.prisma().find_many(take=10)

        # find the first 5 Machine records ordered by the line field
        machines = await Machine.prisma().find_many(
            take=5,
            order={
                'line': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.MachineWhereInput] = None,
        cursor: Optional[types.MachineWhereUniqueInput] = None,
        include: Optional[types.MachineInclude] = None,
        order: Optional[Union[types.MachineOrderByInput, List[types.MachineOrderByInput]]] = None,
        distinct: Optional[List[types.MachineScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Machine record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Machine filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Machine model
        order
            Order the returned Machine records by any field
        distinct
            Filter Machine records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Machine
            The first Machine record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Machine record ordered by the location field
        machine = await Machine.prisma().find_first(
            skip=1,
            order={
                'location': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.MachineWhereInput] = None,
        cursor: Optional[types.MachineWhereUniqueInput] = None,
        include: Optional[types.MachineInclude] = None,
        order: Optional[Union[types.MachineOrderByInput, List[types.MachineOrderByInput]]] = None,
        distinct: Optional[List[types.MachineScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Machine record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Machine filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Machine model
        order
            Order the returned Machine records by any field
        distinct
            Filter Machine records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Machine
            The first Machine record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Machine record ordered by the baseHealth field
        machine = await Machine.prisma().find_first_or_raise(
            skip=1,
            order={
                'baseHealth': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.MachineUpdateInput,
        where: types.MachineWhereUniqueInput,
        include: Optional[types.MachineInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Machine record.

        Parameters
        ----------
        data
            Machine record data specifying what to update
        where
            Machine filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Machine model

        Returns
        -------
        prisma.models.Machine
            The updated Machine record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        machine = await Machine.prisma().update(
            where={
                'id': 'bagcfbhiig',
            },
            data={
                # data to update the Machine record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.MachineWhereUniqueInput,
        data: types.MachineUpsertInput,
        include: Optional[types.MachineInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Machine filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Machine model

        Returns
        -------
        prisma.models.Machine
            The created or updated Machine record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        machine = await Machine.prisma().upsert(
            where={
                'id': 'cghideieh',
            },
            data={
                'create': {
                    'id': 'cghideieh',
                    'name': 'cadfabfehe',
                },
                'update': {
                    'name': 'cadfabfehe',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.MachineUpdateManyMutationInput,
        where: types.MachineWhereInput,
    ) -> int:
        """Update multiple Machine records

        Parameters
        ----------
        data
            Machine data to update the selected Machine records to
        where
            Filter to select the Machine records to update

        Returns
        -------
        int
            The total number of Machine records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Machine records
        total = await Machine.prisma().update_many(
            data={
                'status': 'biabhbdai'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.MachineWhereInput] = None,
        cursor: Optional[types.MachineWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Machine records present in the database

        Parameters
        ----------
        select
            Select the Machine fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Machine filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.MachineCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Machine.prisma().count()

        # results: prisma.types.MachineCountAggregateOutput
        results = await Machine.prisma().count(
            select={
                '_all': True,
                'installedAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.MachineCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.MachineWhereInput] = None,
        cursor: Optional[types.MachineWhereUniqueInput] = None,
    ) -> types.MachineCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.MachineCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.MachineWhereInput] = None,
        cursor: Optional[types.MachineWhereUniqueInput] = None,
    ) -> Union[int, types.MachineCountAggregateOutput]:
        """Count the number of Machine records present in the database

        Parameters
        ----------
        select
            Select the Machine fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Machine filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.MachineCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Machine.prisma().count()

        # results: prisma.types.MachineCountAggregateOutput
        results = await Machine.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.MachineCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.MachineWhereInput] = None
    ) -> int:
        """Delete multiple Machine records.

        Parameters
        ----------
        where
            Optional Machine filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Machine records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Machine records
        total = await Machine.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.MachineScalarFieldKeys'],
        *,
        where: Optional['types.MachineWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.MachineAvgAggregateInput'] = None,
        sum: Optional['types.MachineSumAggregateInput'] = None,
        min: Optional['types.MachineMinAggregateInput'] = None,
        max: Optional['types.MachineMaxAggregateInput'] = None,
        having: Optional['types.MachineScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.MachineCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.MachineScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.MachineScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.MachineGroupByOutput']:
        """Group Machine records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Machine fields to group records by
        where
            Machine filter to select records
        take
            Limit the maximum number of Machine records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.MachineGroupByOutput]
            A list of dictionaries representing the Machine record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Machine records by id values
        # and count how many records are in each group
        results = await Machine.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class SensorReadingActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.SensorReading]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await SensorReading.prisma().query_raw(
            'SELECT * FROM SensorReading WHERE id = ?',
            'idghgaicb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.SensorReading
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await SensorReading.prisma().query_first(
            'SELECT * FROM SensorReading WHERE time = ?',
            datetime.datetime.utcnow(),
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.SensorReadingCreateInput,
        include: Optional[types.SensorReadingInclude] = None
    ) -> _PrismaModelT:
        """Create a new SensorReading record.

        Parameters
        ----------
        data
            SensorReading record data
        include
            Specifies which relations should be loaded on the returned SensorReading model

        Returns
        -------
        prisma.models.SensorReading
            The created SensorReading record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a SensorReading record from just the required fields
        sensorreading = await SensorReading.prisma().create(
            data={
                # data to create a SensorReading record
                'machineId': 'fjfddhigg',
                'temperature': 790425851.211191,
                'vibration': 1149758321.164428,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.SensorReadingCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple SensorReading records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of SensorReading record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await SensorReading.prisma().create_many(
            data=[
                {
                    # data to create a SensorReading record
                    'machineId': 'bdiicjafbj',
                    'temperature': 1647418052.167554,
                    'vibration': 1767274722.32627,
                },
                {
                    # data to create a SensorReading record
                    'machineId': 'bdedcabahc',
                    'temperature': 675780521.74496,
                    'vibration': 1969681615.111617,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.SensorReadingWhereUniqueInput,
        include: Optional[types.SensorReadingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single SensorReading record.

        Parameters
        ----------
        where
            SensorReading filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned SensorReading model

        Returns
        -------
        prisma.models.SensorReading
            The deleted SensorReading record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sensorreading = await SensorReading.prisma().delete(
            where={
                'id': 'igbehcbab',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.SensorReadingWhereUniqueInput,
        include: Optional[types.SensorReadingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique SensorReading record.

        Parameters
        ----------
        where
            SensorReading filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned SensorReading model

        Returns
        -------
        prisma.models.SensorReading
            The found SensorReading record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sensorreading = await SensorReading.prisma().find_unique(
            where={
                'id': 'bdadaadhag',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.SensorReadingWhereUniqueInput,
        include: Optional[types.SensorReadingInclude] = None
    ) -> _PrismaModelT:
        """Find a unique SensorReading record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            SensorReading filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned SensorReading model

        Returns
        -------
        prisma.models.SensorReading
            The found SensorReading record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sensorreading = await SensorReading.prisma().find_unique_or_raise(
            where={
                'id': 'bgiggdidbf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SensorReadingWhereInput] = None,
        cursor: Optional[types.SensorReadingWhereUniqueInput] = None,
        include: Optional[types.SensorReadingInclude] = None,
        order: Optional[Union[types.SensorReadingOrderByInput, List[types.SensorReadingOrderByInput]]] = None,
        distinct: Optional[List[types.SensorReadingScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple SensorReading records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of SensorReading records returned
        skip
            Ignore the first N results
        where
            SensorReading filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SensorReading model
        order
            Order the returned SensorReading records by any field
        distinct
            Filter SensorReading records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.SensorReading]
            The list of all SensorReading records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 SensorReading records
        sensorreadings = await SensorReading.prisma().find_many(take=10)

        # find the first 5 SensorReading records ordered by the machineId field
        sensorreadings = await SensorReading.prisma().find_many(
            take=5,
            order={
                'machineId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SensorReadingWhereInput] = None,
        cursor: Optional[types.SensorReadingWhereUniqueInput] = None,
        include: Optional[types.SensorReadingInclude] = None,
        order: Optional[Union[types.SensorReadingOrderByInput, List[types.SensorReadingOrderByInput]]] = None,
        distinct: Optional[List[types.SensorReadingScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single SensorReading record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            SensorReading filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SensorReading model
        order
            Order the returned SensorReading records by any field
        distinct
            Filter SensorReading records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.SensorReading
            The first SensorReading record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second SensorReading record ordered by the temperature field
        sensorreading = await SensorReading.prisma().find_first(
            skip=1,
            order={
                'temperature': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SensorReadingWhereInput] = None,
        cursor: Optional[types.SensorReadingWhereUniqueInput] = None,
        include: Optional[types.SensorReadingInclude] = None,
        order: Optional[Union[types.SensorReadingOrderByInput, List[types.SensorReadingOrderByInput]]] = None,
        distinct: Optional[List[types.SensorReadingScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single SensorReading record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            SensorReading filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SensorReading model
        order
            Order the returned SensorReading records by any field
        distinct
            Filter SensorReading records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.SensorReading
            The first SensorReading record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second SensorReading record ordered by the vibration field
        sensorreading = await SensorReading.prisma().find_first_or_raise(
            skip=1,
            order={
                'vibration': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.SensorReadingUpdateInput,
        where: types.SensorReadingWhereUniqueInput,
        include: Optional[types.SensorReadingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single SensorReading record.

        Parameters
        ----------
        data
            SensorReading record data specifying what to update
        where
            SensorReading filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned SensorReading model

        Returns
        -------
        prisma.models.SensorReading
            The updated SensorReading record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        sensorreading = await SensorReading.prisma().update(
            where={
                'id': 'caaaedabfc',
            },
            data={
                # data to update the SensorReading record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.SensorReadingWhereUniqueInput,
        data: types.SensorReadingUpsertInput,
        include: Optional[types.SensorReadingInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            SensorReading filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned SensorReading model

        Returns
        -------
        prisma.models.SensorReading
            The created or updated SensorReading record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sensorreading = await SensorReading.prisma().upsert(
            where={
                'id': 'bigibebcib',
            },
            data={
                'create': {
                    'id': 'bigibebcib',
                    'machineId': 'bdedcabahc',
                    'temperature': 675780521.74496,
                    'vibration': 1969681615.111617,
                },
                'update': {
                    'machineId': 'bdedcabahc',
                    'temperature': 675780521.74496,
                    'vibration': 1969681615.111617,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.SensorReadingUpdateManyMutationInput,
        where: types.SensorReadingWhereInput,
    ) -> int:
        """Update multiple SensorReading records

        Parameters
        ----------
        data
            SensorReading data to update the selected SensorReading records to
        where
            Filter to select the SensorReading records to update

        Returns
        -------
        int
            The total number of SensorReading records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all SensorReading records
        total = await SensorReading.prisma().update_many(
            data={
                'rpm': 1860847622
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SensorReadingWhereInput] = None,
        cursor: Optional[types.SensorReadingWhereUniqueInput] = None,
    ) -> int:
        """Count the number of SensorReading records present in the database

        Parameters
        ----------
        select
            Select the SensorReading fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            SensorReading filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SensorReadingCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await SensorReading.prisma().count()

        # results: prisma.types.SensorReadingCountAggregateOutput
        results = await SensorReading.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.SensorReadingCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SensorReadingWhereInput] = None,
        cursor: Optional[types.SensorReadingWhereUniqueInput] = None,
    ) -> types.SensorReadingCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.SensorReadingCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SensorReadingWhereInput] = None,
        cursor: Optional[types.SensorReadingWhereUniqueInput] = None,
    ) -> Union[int, types.SensorReadingCountAggregateOutput]:
        """Count the number of SensorReading records present in the database

        Parameters
        ----------
        select
            Select the SensorReading fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            SensorReading filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SensorReadingCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await SensorReading.prisma().count()

        # results: prisma.types.SensorReadingCountAggregateOutput
        results = await SensorReading.prisma().count(
            select={
                '_all': True,
                'time': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.SensorReadingCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.SensorReadingWhereInput] = None
    ) -> int:
        """Delete multiple SensorReading records.

        Parameters
        ----------
        where
            Optional SensorReading filter to find the records to be deleted

        Returns
        -------
        int
            The total number of SensorReading records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all SensorReading records
        total = await SensorReading.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.SensorReadingScalarFieldKeys'],
        *,
        where: Optional['types.SensorReadingWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.SensorReadingAvgAggregateInput'] = None,
        sum: Optional['types.SensorReadingSumAggregateInput'] = None,
        min: Optional['types.SensorReadingMinAggregateInput'] = None,
        max: Optional['types.SensorReadingMaxAggregateInput'] = None,
        having: Optional['types.SensorReadingScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.SensorReadingCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.SensorReadingScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.SensorReadingScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.SensorReadingGroupByOutput']:
        """Group SensorReading records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar SensorReading fields to group records by
        where
            SensorReading filter to select records
        take
            Limit the maximum number of SensorReading records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.SensorReadingGroupByOutput]
            A list of dictionaries representing the SensorReading record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group SensorReading records by machineId values
        # and count how many records are in each group
        results = await SensorReading.prisma().group_by(
            ['machineId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FailureFingerprintActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.FailureFingerprint]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await FailureFingerprint.prisma().query_raw(
            'SELECT * FROM FailureFingerprint WHERE id = ?',
            'beeifcbebf',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.FailureFingerprint
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await FailureFingerprint.prisma().query_first(
            'SELECT * FROM FailureFingerprint WHERE issueName = ?',
            'bgcigfahea',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.FailureFingerprintCreateInput,
        include: Optional[types.FailureFingerprintInclude] = None
    ) -> _PrismaModelT:
        """Create a new FailureFingerprint record.

        Parameters
        ----------
        data
            FailureFingerprint record data
        include
            Specifies which relations should be loaded on the returned FailureFingerprint model

        Returns
        -------
        prisma.models.FailureFingerprint
            The created FailureFingerprint record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a FailureFingerprint record from just the required fields
        failurefingerprint = await FailureFingerprint.prisma().create(
            data={
                # data to create a FailureFingerprint record
                'issueName': 'bcejgaggif',
                'severity': 'idfjadbcc',
                'patternData': 'hgdhbjhhj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.FailureFingerprintCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple FailureFingerprint records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of FailureFingerprint record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await FailureFingerprint.prisma().create_many(
            data=[
                {
                    # data to create a FailureFingerprint record
                    'issueName': 'ecjjjfbae',
                    'severity': 'bhhfibbigf',
                    'patternData': 'ijdbeffgg',
                },
                {
                    # data to create a FailureFingerprint record
                    'issueName': 'jjfeafhfj',
                    'severity': 'cbachdgfce',
                    'patternData': 'chbfcacbd',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.FailureFingerprintWhereUniqueInput,
        include: Optional[types.FailureFingerprintInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single FailureFingerprint record.

        Parameters
        ----------
        where
            FailureFingerprint filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned FailureFingerprint model

        Returns
        -------
        prisma.models.FailureFingerprint
            The deleted FailureFingerprint record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        failurefingerprint = await FailureFingerprint.prisma().delete(
            where={
                'id': 'efggddide',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.FailureFingerprintWhereUniqueInput,
        include: Optional[types.FailureFingerprintInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique FailureFingerprint record.

        Parameters
        ----------
        where
            FailureFingerprint filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FailureFingerprint model

        Returns
        -------
        prisma.models.FailureFingerprint
            The found FailureFingerprint record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        failurefingerprint = await FailureFingerprint.prisma().find_unique(
            where={
                'id': 'caficfigfb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.FailureFingerprintWhereUniqueInput,
        include: Optional[types.FailureFingerprintInclude] = None
    ) -> _PrismaModelT:
        """Find a unique FailureFingerprint record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            FailureFingerprint filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FailureFingerprint model

        Returns
        -------
        prisma.models.FailureFingerprint
            The found FailureFingerprint record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        failurefingerprint = await FailureFingerprint.prisma().find_unique_or_raise(
            where={
                'id': 'bfidgijfjc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FailureFingerprintWhereInput] = None,
        cursor: Optional[types.FailureFingerprintWhereUniqueInput] = None,
        include: Optional[types.FailureFingerprintInclude] = None,
        order: Optional[Union[types.FailureFingerprintOrderByInput, List[types.FailureFingerprintOrderByInput]]] = None,
        distinct: Optional[List[types.FailureFingerprintScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple FailureFingerprint records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of FailureFingerprint records returned
        skip
            Ignore the first N results
        where
            FailureFingerprint filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FailureFingerprint model
        order
            Order the returned FailureFingerprint records by any field
        distinct
            Filter FailureFingerprint records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.FailureFingerprint]
            The list of all FailureFingerprint records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 FailureFingerprint records
        failurefingerprints = await FailureFingerprint.prisma().find_many(take=10)

        # find the first 5 FailureFingerprint records ordered by the severity field
        failurefingerprints = await FailureFingerprint.prisma().find_many(
            take=5,
            order={
                'severity': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FailureFingerprintWhereInput] = None,
        cursor: Optional[types.FailureFingerprintWhereUniqueInput] = None,
        include: Optional[types.FailureFingerprintInclude] = None,
        order: Optional[Union[types.FailureFingerprintOrderByInput, List[types.FailureFingerprintOrderByInput]]] = None,
        distinct: Optional[List[types.FailureFingerprintScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single FailureFingerprint record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FailureFingerprint filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FailureFingerprint model
        order
            Order the returned FailureFingerprint records by any field
        distinct
            Filter FailureFingerprint records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FailureFingerprint
            The first FailureFingerprint record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FailureFingerprint record ordered by the patternData field
        failurefingerprint = await FailureFingerprint.prisma().find_first(
            skip=1,
            order={
                'patternData': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FailureFingerprintWhereInput] = None,
        cursor: Optional[types.FailureFingerprintWhereUniqueInput] = None,
        include: Optional[types.FailureFingerprintInclude] = None,
        order: Optional[Union[types.FailureFingerprintOrderByInput, List[types.FailureFingerprintOrderByInput]]] = None,
        distinct: Optional[List[types.FailureFingerprintScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single FailureFingerprint record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FailureFingerprint filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FailureFingerprint model
        order
            Order the returned FailureFingerprint records by any field
        distinct
            Filter FailureFingerprint records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FailureFingerprint
            The first FailureFingerprint record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FailureFingerprint record ordered by the etaDays field
        failurefingerprint = await FailureFingerprint.prisma().find_first_or_raise(
            skip=1,
            order={
                'etaDays': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.FailureFingerprintUpdateInput,
        where: types.FailureFingerprintWhereUniqueInput,
        include: Optional[types.FailureFingerprintInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single FailureFingerprint record.

        Parameters
        ----------
        data
            FailureFingerprint record data specifying what to update
        where
            FailureFingerprint filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned FailureFingerprint model

        Returns
        -------
        prisma.models.FailureFingerprint
            The updated FailureFingerprint record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        failurefingerprint = await FailureFingerprint.prisma().update(
            where={
                'id': 'ihieecagf',
            },
            data={
                # data to update the FailureFingerprint record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.FailureFingerprintWhereUniqueInput,
        data: types.FailureFingerprintUpsertInput,
        include: Optional[types.FailureFingerprintInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            FailureFingerprint filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned FailureFingerprint model

        Returns
        -------
        prisma.models.FailureFingerprint
            The created or updated FailureFingerprint record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        failurefingerprint = await FailureFingerprint.prisma().upsert(
            where={
                'id': 'bghfciaafe',
            },
            data={
                'create': {
                    'id': 'bghfciaafe',
                    'issueName': 'jjfeafhfj',
                    'severity': 'cbachdgfce',
                    'patternData': 'chbfcacbd',
                },
                'update': {
                    'issueName': 'jjfeafhfj',
                    'severity': 'cbachdgfce',
                    'patternData': 'chbfcacbd',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.FailureFingerprintUpdateManyMutationInput,
        where: types.FailureFingerprintWhereInput,
    ) -> int:
        """Update multiple FailureFingerprint records

        Parameters
        ----------
        data
            FailureFingerprint data to update the selected FailureFingerprint records to
        where
            Filter to select the FailureFingerprint records to update

        Returns
        -------
        int
            The total number of FailureFingerprint records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all FailureFingerprint records
        total = await FailureFingerprint.prisma().update_many(
            data={
                'actionPrescription': 'bgchfhgceh'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FailureFingerprintWhereInput] = None,
        cursor: Optional[types.FailureFingerprintWhereUniqueInput] = None,
    ) -> int:
        """Count the number of FailureFingerprint records present in the database

        Parameters
        ----------
        select
            Select the FailureFingerprint fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FailureFingerprint filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FailureFingerprintCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await FailureFingerprint.prisma().count()

        # results: prisma.types.FailureFingerprintCountAggregateOutput
        results = await FailureFingerprint.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.FailureFingerprintCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FailureFingerprintWhereInput] = None,
        cursor: Optional[types.FailureFingerprintWhereUniqueInput] = None,
    ) -> types.FailureFingerprintCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.FailureFingerprintCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FailureFingerprintWhereInput] = None,
        cursor: Optional[types.FailureFingerprintWhereUniqueInput] = None,
    ) -> Union[int, types.FailureFingerprintCountAggregateOutput]:
        """Count the number of FailureFingerprint records present in the database

        Parameters
        ----------
        select
            Select the FailureFingerprint fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FailureFingerprint filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FailureFingerprintCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await FailureFingerprint.prisma().count()

        # results: prisma.types.FailureFingerprintCountAggregateOutput
        results = await FailureFingerprint.prisma().count(
            select={
                '_all': True,
                'issueName': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FailureFingerprintCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.FailureFingerprintWhereInput] = None
    ) -> int:
        """Delete multiple FailureFingerprint records.

        Parameters
        ----------
        where
            Optional FailureFingerprint filter to find the records to be deleted

        Returns
        -------
        int
            The total number of FailureFingerprint records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all FailureFingerprint records
        total = await FailureFingerprint.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.FailureFingerprintScalarFieldKeys'],
        *,
        where: Optional['types.FailureFingerprintWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FailureFingerprintAvgAggregateInput'] = None,
        sum: Optional['types.FailureFingerprintSumAggregateInput'] = None,
        min: Optional['types.FailureFingerprintMinAggregateInput'] = None,
        max: Optional['types.FailureFingerprintMaxAggregateInput'] = None,
        having: Optional['types.FailureFingerprintScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FailureFingerprintCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FailureFingerprintScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FailureFingerprintScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FailureFingerprintGroupByOutput']:
        """Group FailureFingerprint records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar FailureFingerprint fields to group records by
        where
            FailureFingerprint filter to select records
        take
            Limit the maximum number of FailureFingerprint records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FailureFingerprintGroupByOutput]
            A list of dictionaries representing the FailureFingerprint record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group FailureFingerprint records by severity values
        # and count how many records are in each group
        results = await FailureFingerprint.prisma().group_by(
            ['severity'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AlertActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Alert]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Alert.prisma().query_raw(
            'SELECT * FROM Alert WHERE id = ?',
            'cafeiaccbc',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Alert
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Alert.prisma().query_first(
            'SELECT * FROM Alert WHERE machineId = ?',
            'gaddfhfh',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.AlertCreateInput,
        include: Optional[types.AlertInclude] = None
    ) -> _PrismaModelT:
        """Create a new Alert record.

        Parameters
        ----------
        data
            Alert record data
        include
            Specifies which relations should be loaded on the returned Alert model

        Returns
        -------
        prisma.models.Alert
            The created Alert record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Alert record from just the required fields
        alert = await Alert.prisma().create(
            data={
                # data to create a Alert record
                'machineId': 'gieegcbeg',
                'title': 'bgcffadich',
                'severity': 'fcbichhci',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.AlertCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Alert records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Alert record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Alert.prisma().create_many(
            data=[
                {
                    # data to create a Alert record
                    'machineId': 'bcggadccgf',
                    'title': 'jdcfdcgc',
                    'severity': 'cafdaehjid',
                },
                {
                    # data to create a Alert record
                    'machineId': 'gifdddbia',
                    'title': 'bchehecef',
                    'severity': 'jeijcbhfe',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.AlertWhereUniqueInput,
        include: Optional[types.AlertInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Alert record.

        Parameters
        ----------
        where
            Alert filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Alert model

        Returns
        -------
        prisma.models.Alert
            The deleted Alert record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        alert = await Alert.prisma().delete(
            where={
                'id': 'bjgejjabff',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.AlertWhereUniqueInput,
        include: Optional[types.AlertInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Alert record.

        Parameters
        ----------
        where
            Alert filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Alert model

        Returns
        -------
        prisma.models.Alert
            The found Alert record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        alert = await Alert.prisma().find_unique(
            where={
                'id': 'bcciijbibg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.AlertWhereUniqueInput,
        include: Optional[types.AlertInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Alert record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Alert filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Alert model

        Returns
        -------
        prisma.models.Alert
            The found Alert record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        alert = await Alert.prisma().find_unique_or_raise(
            where={
                'id': 'cffcachfd',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AlertWhereInput] = None,
        cursor: Optional[types.AlertWhereUniqueInput] = None,
        include: Optional[types.AlertInclude] = None,
        order: Optional[Union[types.AlertOrderByInput, List[types.AlertOrderByInput]]] = None,
        distinct: Optional[List[types.AlertScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Alert records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Alert records returned
        skip
            Ignore the first N results
        where
            Alert filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Alert model
        order
            Order the returned Alert records by any field
        distinct
            Filter Alert records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Alert]
            The list of all Alert records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Alert records
        alerts = await Alert.prisma().find_many(take=10)

        # find the first 5 Alert records ordered by the timestamp field
        alerts = await Alert.prisma().find_many(
            take=5,
            order={
                'timestamp': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AlertWhereInput] = None,
        cursor: Optional[types.AlertWhereUniqueInput] = None,
        include: Optional[types.AlertInclude] = None,
        order: Optional[Union[types.AlertOrderByInput, List[types.AlertOrderByInput]]] = None,
        distinct: Optional[List[types.AlertScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Alert record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Alert filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Alert model
        order
            Order the returned Alert records by any field
        distinct
            Filter Alert records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Alert
            The first Alert record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Alert record ordered by the title field
        alert = await Alert.prisma().find_first(
            skip=1,
            order={
                'title': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AlertWhereInput] = None,
        cursor: Optional[types.AlertWhereUniqueInput] = None,
        include: Optional[types.AlertInclude] = None,
        order: Optional[Union[types.AlertOrderByInput, List[types.AlertOrderByInput]]] = None,
        distinct: Optional[List[types.AlertScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Alert record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Alert filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Alert model
        order
            Order the returned Alert records by any field
        distinct
            Filter Alert records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Alert
            The first Alert record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Alert record ordered by the severity field
        alert = await Alert.prisma().find_first_or_raise(
            skip=1,
            order={
                'severity': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.AlertUpdateInput,
        where: types.AlertWhereUniqueInput,
        include: Optional[types.AlertInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Alert record.

        Parameters
        ----------
        data
            Alert record data specifying what to update
        where
            Alert filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Alert model

        Returns
        -------
        prisma.models.Alert
            The updated Alert record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        alert = await Alert.prisma().update(
            where={
                'id': 'bccdfhdigc',
            },
            data={
                # data to update the Alert record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.AlertWhereUniqueInput,
        data: types.AlertUpsertInput,
        include: Optional[types.AlertInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Alert filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Alert model

        Returns
        -------
        prisma.models.Alert
            The created or updated Alert record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        alert = await Alert.prisma().upsert(
            where={
                'id': 'febcgjbfj',
            },
            data={
                'create': {
                    'id': 'febcgjbfj',
                    'machineId': 'gifdddbia',
                    'title': 'bchehecef',
                    'severity': 'jeijcbhfe',
                },
                'update': {
                    'machineId': 'gifdddbia',
                    'title': 'bchehecef',
                    'severity': 'jeijcbhfe',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.AlertUpdateManyMutationInput,
        where: types.AlertWhereInput,
    ) -> int:
        """Update multiple Alert records

        Parameters
        ----------
        data
            Alert data to update the selected Alert records to
        where
            Filter to select the Alert records to update

        Returns
        -------
        int
            The total number of Alert records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Alert records
        total = await Alert.prisma().update_many(
            data={
                'confidence': 1064846676
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AlertWhereInput] = None,
        cursor: Optional[types.AlertWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Alert records present in the database

        Parameters
        ----------
        select
            Select the Alert fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Alert filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AlertCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Alert.prisma().count()

        # results: prisma.types.AlertCountAggregateOutput
        results = await Alert.prisma().count(
            select={
                '_all': True,
                'etaDays': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.AlertCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AlertWhereInput] = None,
        cursor: Optional[types.AlertWhereUniqueInput] = None,
    ) -> types.AlertCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.AlertCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AlertWhereInput] = None,
        cursor: Optional[types.AlertWhereUniqueInput] = None,
    ) -> Union[int, types.AlertCountAggregateOutput]:
        """Count the number of Alert records present in the database

        Parameters
        ----------
        select
            Select the Alert fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Alert filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AlertCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Alert.prisma().count()

        # results: prisma.types.AlertCountAggregateOutput
        results = await Alert.prisma().count(
            select={
                '_all': True,
                'action': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AlertCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.AlertWhereInput] = None
    ) -> int:
        """Delete multiple Alert records.

        Parameters
        ----------
        where
            Optional Alert filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Alert records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Alert records
        total = await Alert.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.AlertScalarFieldKeys'],
        *,
        where: Optional['types.AlertWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AlertAvgAggregateInput'] = None,
        sum: Optional['types.AlertSumAggregateInput'] = None,
        min: Optional['types.AlertMinAggregateInput'] = None,
        max: Optional['types.AlertMaxAggregateInput'] = None,
        having: Optional['types.AlertScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AlertCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AlertScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AlertScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AlertGroupByOutput']:
        """Group Alert records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Alert fields to group records by
        where
            Alert filter to select records
        take
            Limit the maximum number of Alert records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AlertGroupByOutput]
            A list of dictionaries representing the Alert record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Alert records by rawDiagnosis values
        # and count how many records are in each group
        results = await Alert.prisma().group_by(
            ['rawDiagnosis'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models